{
  "name": "bluebox",
  "main": "lib/index.js",
  "dependencies": {},
  "devDependencies": {
    "browserify": "",
    "flow-typestrip": "^0.1.3",
    "jest-cli": "^0.4.18"
  },
  "scripts": {
    "test": "jest",
    "build": "browserify -t flow-typestrip examples/Todo/TodoList.js -o build/build.js"
  }
}
