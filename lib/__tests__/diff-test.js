jest.dontMock('../diff.js');

describe('diff', function() {

  it ('should give the old object back when identical to the new object', function(){

  });

  it('should give the new object back when different from old object', function() {

  });

  it('should reuse array values from a different position when the key value is used', function() {

  });

});